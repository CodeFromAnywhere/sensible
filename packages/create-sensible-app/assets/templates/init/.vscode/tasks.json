{
  "version": "2.0.0",
  "options": { "shell": { "executable": "sh", "args": ["-c"] } },
  "tasks": [
    {
      "label": "Welcome",
      "type": "shell",
      "presentation": { "reveal": "never", "close": true },
      "command": "say \"Happy coding. Don't forget to ship and pull, and stay sensible! You got this.\"",
      "runOptions": { "runOn": "folderOpen" }
    },

    {
      "label": "Open sensible docs",
      "type": "shell",
      "presentation": { "reveal": "never", "close": true },
      "command": "open https://docs.sensibleframework.co/localhost:4000",
      "runOptions": { "runOn": "folderOpen" }
    },

    {
      "label": "Watch Core",
      "type": "shell",
      "isBackground": true, //prevent loading indicator ui
      "options": { "cwd": "${workspaceFolder}/packages/core" },
      "presentation": { "group": "run" },
      "command": "yarn dev",
      "runOptions": { "runOn": "folderOpen" }
    },

    {
      "label": "Run server",
      "type": "shell",
      "isBackground": true, //prevent loading indicator ui
      "options": { "cwd": "${workspaceFolder}/apps/server" },
      "presentation": { "group": "run" },
      "command": "yarn dev",
      "runOptions": { "runOn": "folderOpen" }
    },

    {
      "label": "Run web packager",
      "type": "shell",
      "isBackground": true, //prevent loading indicator ui
      "options": { "cwd": "${workspaceFolder}/apps/web" },
      "presentation": { "group": "run" },
      "command": "yarn dev",
      "runOptions": { "runOn": "folderOpen" }
    },

    {
      "label": "Start app packager",
      "type": "shell",
      "isBackground": true, //prevent loading indicator ui
      "options": { "cwd": "${workspaceFolder}/apps/app" },
      "command": "yarn start",
      "presentation": { "group": "run" },
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
